#include "Amino/Client.h"

int main() {
    // Создание объекта класса Client
    Client client;

    // Вызов методов объекта client
    client.doSomething();
    client.doAnotherThing();

    return 0;
}